export type ThemeMode = "dark" | "light";

export type ThemeTokens = {
  mode: ThemeMode;
  background: string;
  surface: string;
  surfaceMuted: string;
  textPrimary: string;
  textSecondary: string;
  tomatoPrimary: string;
  tomatoUnripe: string;
  tomatoSoft: string;
  tomatoGlow: string;
  cream: string;
  border: string;
  lockedAccent: string;
  lockedFill: string;
  shadow: string;
  heatmapDim: string;
};
