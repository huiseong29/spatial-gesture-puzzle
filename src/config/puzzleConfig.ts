export const puzzleConfig = {
  minGridSize: 2,
  defaultGridSize: 3,
  maxGridSize: 4,
  difficultySmoothingAlpha: 0.35,
  smallCaptureAreaRatio: 0.18,
  largeCaptureAreaRatio: 0.42,
  highGestureConfidence: 0.78,
  lowGestureConfidence: 0.46,
  highJitterPx: 72,
  pointerHistoryMaxSamples: 360,
  pointerHistoryMinDistancePx: 4,
  heatmapReplayMs: 2800,
  heatmapPointRadiusPx: 34,
  heatmapDragAlpha: 0.16,
  heatmapHoverAlpha: 0.06,
  pieceGap: 3,
  objectFit: "cover",
  shuffleAttempts: 8,
  snapThresholdPx: 44,
  snapPreviewDistancePx: 80,
  magneticStrength: 0.12,
  correctGlowAlpha: 0.35,
  cellHighlightRadius: 12,
  snapPulseMs: 420,
  completionDisplayMs: 3000,
  autoResetFadeMs: 600,
  lockedOutlineWidth: 2,
  lockedGlowAlpha: 0.18,
  lockedFillAlpha: 0.08,
  lockedBadgeRadius: 0,
  hitPaddingPx: 20,
  grabStartWindowFrames: 4,
  centerGrabRadiusRatio: 0.65,
  dragReleaseGraceFrames: 4,
  pointerLostToleranceFrames: 5,
  activeHandReacquireRadiusPx: 80,
  dragPointerSlowAlpha: 0.18,
  dragPointerFastAlpha: 0.03,
  dragPointerVelocityThreshold: 520,
  transitionGridMs: 360,
  transitionPopMs: 420,
  transitionShuffleMs: 760,
  transitionStaggerMs: 42
} as const;
